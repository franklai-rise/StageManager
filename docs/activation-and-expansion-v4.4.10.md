# v4.4.10 现场修正

- Clash 的通知区图标调用已在本机实测，可以把其窗口恢复并置前，且没有切换 VPN 或代理状态。
- 第一次窗口恢复可能已经清除“最小化”标记，但仍被 Windows 拒绝前台权限；因此通知区回退现在以最终前台验证结果为准，不再依赖最小化标记。
- VPN Manager 的窗口标题包含版本号，而通知图标包含实时线路状态；匹配器会同时使用完整标题和稳定标题前缀。
- 子卡片触碰后立即展开，每张间隔 45 ms、运动 300 ms，并使用平滑的 ease-in/ease-out 曲线，让相邻卡片的运动充分重叠。
- 连接线的绘制、位置和显示时机没有修改。
- 67 项自动回归全部通过。
